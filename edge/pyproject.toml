[project]
name = "edge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "boto3>=1.43.75",
  "pillow>=12.3.0",
  "torch>=2.13.0",
  "torchvision>=0.28.0",
]

[dependency-groups]
dev = ["pytest>=9.1.1"]
